---
import SafePresentationComponent from "../../components/SafePresentation.astro";

const formData = await Astro.request.formData();
const cliente = String(formData.get("cliente") || "");
---
<SafePresentationComponent cliente={cliente} />
